* [#391](https://github.com/workos/workos-kotlin/pull/391) feat(generated)!: regenerate from spec (9 changes)

  **⚠️ Breaking**
  * **user_management:** Remove returnTo parameter from revokeSession
    * Removed `returnTo` parameter from `UserManagement.revokeSession()` method
    * Removed `returnTo` parameter from `UserManagement.revokeSessionSuspend()` method
    * Removed `return_to` field from `RevokeSession` model
  * **user_management:** Add name field to user models
    * Added `name` field to `User` model
    * Added `name` field to `CreateUser` model with new `name` parameter
    * Added `name` field to `UpdateUser` model with new `name` parameter
    * Added `name` field to `UserObject` model
    * Updated `revokeSession()` and `update()` methods to support new `name` parameter
  * **user_management:** Add name field to email change confirmation user
    * Added `name` field to `EmailChangeConfirmationUser` model

  **Features**
  * **api_keys:** Add expire operation for API keys
    * Added `createExpire()` and `createExpireSuspend()` methods to expire API keys immediately or schedule future expiration
    * Added `ExpireApiKey` request model
    * Added `ApiKeyUpdated` event model and `ApiKeyUpdatedData` payload
    * Added `ApiKeyUpdatedDataOwner` and `ApiKeyUpdatedDataPreviousAttribute` models for event payload
    * Updated `CreateWebhookEndpointEvents` enum with `API_KEY_UPDATED` value
    * Updated `UpdateWebhookEndpointEvents` enum with `API_KEY_UPDATED` value
  * **directory_sync:** Remove DsyncDeactivated event and related models
    * Removed `DsyncDeactivated` event model
    * Removed `DsyncDeactivatedData` alias model
    * Removed `DsyncDeactivatedDataDomain` alias model
    * Removed `DsyncDeactivatedDataState` enum
    * Removed `DsyncDeactivatedDataType` enum
  * **directory_sync:** Add token lifecycle events for directory sync
    * Added `DsyncTokenCreated` event model
    * Added `DsyncTokenCreatedData` payload with token details
    * Added `DsyncTokenRevoked` event model
    * Added `DsyncTokenRevokedData` alias to `DsyncTokenCreatedData`
    * Registered new event types in `WorkOSEvent` polymorphic deserialization
  * **radar:** Remove DOMAIN_SIGN_UP_RATE_LIMIT from RadarStandaloneResponseControl
    * Removed `DomainSignUpRateLimit` enum value from `RadarStandaloneResponseControl`
  * **audit_logs:** Add Snowflake to audit log stream types
    * Added `Snowflake` enum value to `AuditLogConfigurationLogStreamType`

  **Fixes**
  * **api_keys:** Make expires_at required in API key created/revoked events
    * Made `expires_at` field required (non-optional) in `ApiKeyCreatedData` event payload
    * Made `expires_at` field required (non-optional) in `ApiKeyRevokedData` event payload
