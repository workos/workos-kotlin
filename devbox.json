{
  "packages": [
    "gradle@7.4.2"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to devbox!' > /dev/null"
    ],
    "scripts": {
      "build": "gradle build --no-daemon",
      "test": "gradle test --info --no-daemon"
    }
  }
}