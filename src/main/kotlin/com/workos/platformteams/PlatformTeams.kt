// This file is auto-generated by oagen. Do not edit.

package com.workos.platformteams

import com.workos.WorkOS
import com.workos.common.http.RequestConfig
import com.workos.common.http.RequestOptions
import com.workos.common.http.bodyOf
import com.workos.common.http.encodePathSegment
import com.workos.models.Team
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/**
 * API accessor for PlatformTeams.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`) and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
 */
class PlatformTeams(
  internal val workos: WorkOS
) {
  /**
   * Create a team
   *
   * Creates a team along with its default project, a staging environment, and a production environment. An admin invitation is sent to `admin_email`, onboarding is marked complete with AuthKit enabled, and the calling platform is authorized to act inside the team.
   *
   * @param adminEmail The email address of the person who will administer the team. An invitation is sent to this address.
   * @param name Name of the team.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the Team
   */
  @JvmOverloads
  fun createTeam(
    adminEmail: String,
    name: String,
    requestOptions: RequestOptions? = null
  ): Team {
    val body =
      bodyOf(
        "admin_email" to adminEmail,
        "name" to name
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/platform/teams",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, Team::class.java)
  }

  /**
   * Coroutine-aware variant of [createTeam]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createTeam] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createTeamSuspend")
  suspend fun createTeamSuspend(
    adminEmail: String,
    name: String,
    requestOptions: RequestOptions? = null
  ): Team =
    withContext(Dispatchers.IO) {
      createTeam(adminEmail, name, requestOptions)
    }

  /**
   * Get a team
   *
   * Returns a team, and doubles as a health check on the platform's access to it. Read `production_state` before attempting to create a production environment.
   *
   * @param teamId The ID of the team.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the Team
   */
  @JvmOverloads
  fun getTeam(
    teamId: String,
    requestOptions: RequestOptions? = null
  ): Team {
    val config =
      RequestConfig(
        method = "GET",
        path = "/platform/teams/${encodePathSegment(teamId)}",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, Team::class.java)
  }

  /**
   * Coroutine-aware variant of [getTeam]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [getTeam] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("getTeamSuspend")
  suspend fun getTeamSuspend(
    teamId: String,
    requestOptions: RequestOptions? = null
  ): Team =
    withContext(Dispatchers.IO) {
      getTeam(teamId, requestOptions)
    }
}
