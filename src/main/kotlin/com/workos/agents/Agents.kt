// This file is auto-generated by oagen. Do not edit.

package com.workos.agents

import com.workos.WorkOS
import com.workos.common.http.PatchField
import com.workos.common.http.RequestConfig
import com.workos.common.http.RequestOptions
import com.workos.common.http.bodyOf
import com.workos.common.http.encodePathSegment
import com.workos.common.http.patchBodyOf
import com.workos.models.AgentAdminLinkClaimAttemptToExternalUserRequestUser
import com.workos.models.AgentCredentialValidation
import com.workos.models.AgentRegistration
import com.workos.models.ClaimViewResponse
import com.workos.types.AgentAdminValidateCredentialRequestType
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/**
 * API accessor for Agents.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`) and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
 */
class Agents(
  internal val workos: WorkOS
) {
  /**
   * Link a claim attempt to an external user
   *
   * Link an external user to a claim attempt and retrieve the code needed for the agent to complete the claim. The user is looked up by external ID; if no user exists, one is created. When the user belongs to multiple organizations, an explicit organization must be provided.
   *
   * @param type The operation to perform on the claim attempt. Currently only `link_external_user` is supported.
   * @param claimAttemptToken The token identifying the claim attempt.
   * @param user The user to attach to the claim attempt, identified by email and external ID.
   * @param organizationId The organization to place the agent in. Required when the user belongs to more than one organization.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the ClaimViewResponse
   */
  @JvmOverloads
  fun updateAttempts(
    type: String,
    claimAttemptToken: String,
    user: AgentAdminLinkClaimAttemptToExternalUserRequestUser,
    organizationId: PatchField<String> = PatchField.Absent,
    requestOptions: RequestOptions? = null
  ): ClaimViewResponse {
    val body =
      patchBodyOf(
        "type" to type,
        "claim_attempt_token" to claimAttemptToken,
        "user" to user,
        "organization_id" to organizationId
      )
    val config =
      RequestConfig(
        method = "PATCH",
        path = "/agents/claims/attempts",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, ClaimViewResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [updateAttempts]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [updateAttempts] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("updateAttemptsSuspend")
  suspend fun updateAttemptsSuspend(
    type: String,
    claimAttemptToken: String,
    user: AgentAdminLinkClaimAttemptToExternalUserRequestUser,
    organizationId: PatchField<String> = PatchField.Absent,
    requestOptions: RequestOptions? = null
  ): ClaimViewResponse =
    withContext(Dispatchers.IO) {
      updateAttempts(type, claimAttemptToken, user, organizationId, requestOptions)
    }

  /**
   * Validate an agent credential
   *
   * Validate an agent credential — an API key or access token — against the environment of the API key used to authenticate the request. This is a read-only check: it never consumes or mutates the credential.
   *
   * @param type The kind of credential being validated — an agent API key or an agent access token.
   * @param credential The credential value to validate: the API key value for `api_key`, or the access token (JWT) for `access_token`.
   * @param audience When provided, the access token's `aud` claim is verified against this value. Tokens issued for a different resource are rejected.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the AgentCredentialValidation
   */
  @JvmOverloads
  fun createValidate(
    type: AgentAdminValidateCredentialRequestType,
    credential: String,
    audience: String? = null,
    requestOptions: RequestOptions? = null
  ): AgentCredentialValidation {
    val body =
      bodyOf(
        "type" to type,
        "credential" to credential,
        "audience" to audience
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/agents/credentials/validate",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, AgentCredentialValidation::class.java)
  }

  /**
   * Coroutine-aware variant of [createValidate]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createValidate] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createValidateSuspend")
  suspend fun createValidateSuspend(
    type: AgentAdminValidateCredentialRequestType,
    credential: String,
    audience: String? = null,
    requestOptions: RequestOptions? = null
  ): AgentCredentialValidation =
    withContext(Dispatchers.IO) {
      createValidate(type, credential, audience, requestOptions)
    }

  /**
   * Get an agent registration
   *
   * Retrieve the details of an agent registration by ID. The registration is scoped to the environment of the API key used to authenticate the request.
   *
   * @param id The unique ID of the agent registration.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the AgentRegistration
   */
  @JvmOverloads
  fun getRegistration(
    id: String,
    requestOptions: RequestOptions? = null
  ): AgentRegistration {
    val config =
      RequestConfig(
        method = "GET",
        path = "/agents/registrations/${encodePathSegment(id)}",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, AgentRegistration::class.java)
  }

  /**
   * Coroutine-aware variant of [getRegistration]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [getRegistration] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("getRegistrationSuspend")
  suspend fun getRegistrationSuspend(
    id: String,
    requestOptions: RequestOptions? = null
  ): AgentRegistration =
    withContext(Dispatchers.IO) {
      getRegistration(id, requestOptions)
    }
}
