// This file is auto-generated by oagen. Do not edit.

package com.workos.pipesprovider

import com.workos.WorkOS
import com.workos.common.http.RequestConfig
import com.workos.common.http.RequestOptions
import com.workos.common.http.bodyOf
import com.workos.common.http.encodePathSegment
import com.workos.models.DataIntegrationConfigurationListResponse
import com.workos.models.DataIntegrationConfigurationResponse
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/**
 * API accessor for PipesProvider.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`) and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
 */
class PipesProvider(
  internal val workos: WorkOS
) {
  /**
   * List providers for an organization
   *
   * Returns a list of all providers available to the specified organization, along with any configured custom OAuth scopes, enabled state, and organization-managed credentials where applicable.
   *
   * @param organizationId An [Organization](https://workos.com/docs/reference/organization) identifier to list provider configurations for.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the DataIntegrationConfigurationListResponse
   */
  @JvmOverloads
  fun listOrganizationDataIntegrationConfigurations(
    organizationId: String,
    requestOptions: RequestOptions? = null
  ): DataIntegrationConfigurationListResponse {
    val config =
      RequestConfig(
        method = "GET",
        path = "/organizations/${encodePathSegment(organizationId)}/data_integration_configurations",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, DataIntegrationConfigurationListResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [listOrganizationDataIntegrationConfigurations]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [listOrganizationDataIntegrationConfigurations] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("listOrganizationDataIntegrationConfigurationsSuspend")
  suspend fun listOrganizationDataIntegrationConfigurationsSuspend(
    organizationId: String,
    requestOptions: RequestOptions? = null
  ): DataIntegrationConfigurationListResponse =
    withContext(Dispatchers.IO) {
      listOrganizationDataIntegrationConfigurations(organizationId, requestOptions)
    }

  /**
   * Configure a provider for an organization
   *
   * Creates or updates an organization's provider configuration. Use this endpoint to enable or disable a provider, set custom OAuth scopes, or supply organization-managed OAuth credentials.
   *
   * @param organizationId An [Organization](https://workos.com/docs/reference/organization) identifier to configure the provider for.
   * @param slug The slug identifier of the provider to configure (e.g., `github`, `slack`, `notion`).
   * @param enabled Whether the provider is enabled for the organization.
   * @param scopes The OAuth scopes to request for the organization. Pass `null` to inherit the provider scopes.
   * @param clientId The OAuth client ID of the organization's own application. Must be provided together with `client_secret`, and only for providers whose credentials are supplied by the organization.
   * @param clientSecret The OAuth client secret of the organization's own application. Must be provided together with `client_id`.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the DataIntegrationConfigurationResponse
   */
  @JvmOverloads
  fun updateOrganizationDataIntegrationConfiguration(
    organizationId: String,
    slug: String,
    enabled: Boolean? = null,
    scopes: List<String>? = null,
    clientId: String? = null,
    clientSecret: String? = null,
    requestOptions: RequestOptions? = null
  ): DataIntegrationConfigurationResponse {
    val body =
      bodyOf(
        "enabled" to enabled,
        "scopes" to scopes,
        "client_id" to clientId,
        "client_secret" to clientSecret
      )
    val config =
      RequestConfig(
        method = "PUT",
        path = "/organizations/${encodePathSegment(organizationId)}/data_integration_configurations/${encodePathSegment(slug)}",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, DataIntegrationConfigurationResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [updateOrganizationDataIntegrationConfiguration]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [updateOrganizationDataIntegrationConfiguration] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("updateOrganizationDataIntegrationConfigurationSuspend")
  suspend fun updateOrganizationDataIntegrationConfigurationSuspend(
    organizationId: String,
    slug: String,
    enabled: Boolean? = null,
    scopes: List<String>? = null,
    clientId: String? = null,
    clientSecret: String? = null,
    requestOptions: RequestOptions? = null
  ): DataIntegrationConfigurationResponse =
    withContext(Dispatchers.IO) {
      updateOrganizationDataIntegrationConfiguration(organizationId, slug, enabled, scopes, clientId, clientSecret, requestOptions)
    }
}
