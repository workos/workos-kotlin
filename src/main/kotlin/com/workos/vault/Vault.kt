// This file is auto-generated by oagen. Do not edit.

package com.workos.vault

import com.fasterxml.jackson.core.type.TypeReference
import com.workos.WorkOS
import com.workos.common.http.Page
import com.workos.common.http.RequestConfig
import com.workos.common.http.RequestOptions
import com.workos.common.http.addIfNotNull
import com.workos.common.http.bodyOf
import com.workos.common.http.encodePathSegment
import com.workos.models.CreateDataKeyResponse
import com.workos.models.DecryptResponse
import com.workos.models.Object
import com.workos.models.ObjectMetadata
import com.workos.models.ObjectSummary
import com.workos.models.ObjectWithoutValue
import com.workos.models.VersionListResponse
import com.workos.types.SortOrder
import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext
import java.time.OffsetDateTime

/**
 * API accessor for Vault.
 *
 * Every operation on this class is available in two flavors: a blocking variant (`<methodName>`) and a coroutine-aware variant (`<methodName>Suspend`). The `Suspend` variants delegate to the blocking ones under `withContext(Dispatchers.IO)`, so they are safe to call from any coroutine dispatcher (including `Dispatchers.Main`).
 */
class Vault(
  internal val workos: WorkOS
) {
  /**
   * Create a data key
   *
   * Generate an isolated encryption key for local encryption operations.
   *
   * @param context Map of values used to determine the encryption key.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the CreateDataKeyResponse
   */
  @JvmOverloads
  fun createDataKey(
    context: Map<String, String>,
    requestOptions: RequestOptions? = null
  ): CreateDataKeyResponse {
    val body =
      bodyOf(
        "context" to context
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/vault/v1/keys/data-key",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, CreateDataKeyResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [createDataKey]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createDataKey] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createDataKeySuspend")
  suspend fun createDataKeySuspend(
    context: Map<String, String>,
    requestOptions: RequestOptions? = null
  ): CreateDataKeyResponse =
    withContext(Dispatchers.IO) {
      createDataKey(context, requestOptions)
    }

  /**
   * Decrypt a data key
   *
   * Decrypt a previously encrypted data key from WorkOS Vault.
   *
   * @param keys Base64-encoded encrypted data key to decrypt.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the DecryptResponse
   */
  @JvmOverloads
  fun createDecrypt(
    keys: String,
    requestOptions: RequestOptions? = null
  ): DecryptResponse {
    val body =
      bodyOf(
        "keys" to keys
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/vault/v1/keys/decrypt",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, DecryptResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [createDecrypt]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createDecrypt] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createDecryptSuspend")
  suspend fun createDecryptSuspend(
    keys: String,
    requestOptions: RequestOptions? = null
  ): DecryptResponse =
    withContext(Dispatchers.IO) {
      createDecrypt(keys, requestOptions)
    }

  /**
   * Re-encrypt a data key
   *
   * Decrypt an existing data key and re-encrypt it under a new key context.
   *
   * @param context Map of values used to determine the new encryption key.
   * @param encryptedKeys Base64-encoded encrypted data key blob to re-encrypt.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the CreateDataKeyResponse
   */
  @JvmOverloads
  fun createRekey(
    context: Map<String, String>,
    encryptedKeys: String,
    requestOptions: RequestOptions? = null
  ): CreateDataKeyResponse {
    val body =
      bodyOf(
        "context" to context,
        "encrypted_keys" to encryptedKeys
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/vault/v1/keys/rekey",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, CreateDataKeyResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [createRekey]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createRekey] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createRekeySuspend")
  suspend fun createRekeySuspend(
    context: Map<String, String>,
    encryptedKeys: String,
    requestOptions: RequestOptions? = null
  ): CreateDataKeyResponse =
    withContext(Dispatchers.IO) {
      createRekey(context, encryptedKeys, requestOptions)
    }

  /**
   * List objects
   *
   * List all encrypted objects with cursor-based pagination.
   *
   * @param limit Upper limit on the number of objects to return.
   * @param before Cursor for the previous page of results.
   * @param after Cursor for the next page of results.
   * @param order Sort direction for results.
   * @param search Filter results by name or structured search JSON.
   * @param updatedAfter ISO 8601 timestamp to filter by last modified time.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return a [com.workos.common.http.Page] of results
   */
  @JvmOverloads
  fun listKv(
    limit: Int? = null,
    before: String? = null,
    after: String? = null,
    order: SortOrder? = null,
    search: String? = null,
    updatedAfter: OffsetDateTime? = null,
    requestOptions: RequestOptions? = null
  ): Page<ObjectSummary> {
    val itemType = object : TypeReference<ObjectSummary>() {}
    return workos.baseClient.requestPage(
      method = "GET",
      path = "/vault/v1/kv",
      itemType = itemType,
      requestOptions = requestOptions,
      before = before,
      after = after
    ) {
      limit?.let { add("limit" to it.toString()) }
      order?.let { add("order" to it.value) }
      addIfNotNull("search", search)
      updatedAfter?.let { add("updatedAfter" to it.toString()) }
    }
  }

  /**
   * Coroutine-aware variant of [listKv]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [listKv] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("listKvSuspend")
  suspend fun listKvSuspend(
    limit: Int? = null,
    before: String? = null,
    after: String? = null,
    order: SortOrder? = null,
    search: String? = null,
    updatedAfter: OffsetDateTime? = null,
    requestOptions: RequestOptions? = null
  ): Page<ObjectSummary> =
    withContext(Dispatchers.IO) {
      listKv(limit, before, after, order, search, updatedAfter, requestOptions)
    }

  /**
   * Create an object
   *
   * Encrypt and store a new key-value object.
   *
   * @param keyContext Map of values used to determine the encryption key.
   * @param name Unique name for the object.
   * @param value Plaintext data to encrypt and store.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the ObjectMetadata
   */
  @JvmOverloads
  fun createKv(
    keyContext: Map<String, String>,
    name: String,
    value: String,
    requestOptions: RequestOptions? = null
  ): ObjectMetadata {
    val body =
      bodyOf(
        "key_context" to keyContext,
        "name" to name,
        "value" to value
      )
    val config =
      RequestConfig(
        method = "POST",
        path = "/vault/v1/kv",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, ObjectMetadata::class.java)
  }

  /**
   * Coroutine-aware variant of [createKv]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [createKv] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("createKvSuspend")
  suspend fun createKvSuspend(
    keyContext: Map<String, String>,
    name: String,
    value: String,
    requestOptions: RequestOptions? = null
  ): ObjectMetadata =
    withContext(Dispatchers.IO) {
      createKv(keyContext, name, value, requestOptions)
    }

  /**
   * Read an object by name
   *
   * Fetch and decrypt an object by its unique name.
   *
   * @param name Unique name of the object.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the Object
   */
  @JvmOverloads
  fun getName(
    name: String,
    requestOptions: RequestOptions? = null
  ): Object {
    val config =
      RequestConfig(
        method = "GET",
        path = "/vault/v1/kv/name/${encodePathSegment(name)}",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, Object::class.java)
  }

  /**
   * Coroutine-aware variant of [getName]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [getName] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("getNameSuspend")
  suspend fun getNameSuspend(
    name: String,
    requestOptions: RequestOptions? = null
  ): Object =
    withContext(Dispatchers.IO) {
      getName(name, requestOptions)
    }

  /**
   * Read an object by ID
   *
   * Fetch and decrypt an object by its unique identifier.
   *
   * @param id Unique identifier of the object.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the Object
   */
  @JvmOverloads
  fun getKv(
    id: String,
    requestOptions: RequestOptions? = null
  ): Object {
    val config =
      RequestConfig(
        method = "GET",
        path = "/vault/v1/kv/${encodePathSegment(id)}",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, Object::class.java)
  }

  /**
   * Coroutine-aware variant of [getKv]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [getKv] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("getKvSuspend")
  suspend fun getKvSuspend(
    id: String,
    requestOptions: RequestOptions? = null
  ): Object =
    withContext(Dispatchers.IO) {
      getKv(id, requestOptions)
    }

  /**
   * Update an object
   *
   * Update the value of an existing encrypted object.
   *
   * @param id Unique identifier of the object.
   * @param value New plaintext value.
   * @param versionCheck ID of the expected current version for optimistic locking.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the ObjectWithoutValue
   */
  @JvmOverloads
  fun updateKv(
    id: String,
    value: String,
    versionCheck: String? = null,
    requestOptions: RequestOptions? = null
  ): ObjectWithoutValue {
    val body =
      bodyOf(
        "value" to value,
        "version_check" to versionCheck
      )
    val config =
      RequestConfig(
        method = "PUT",
        path = "/vault/v1/kv/${encodePathSegment(id)}",
        body = body,
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, ObjectWithoutValue::class.java)
  }

  /**
   * Coroutine-aware variant of [updateKv]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [updateKv] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("updateKvSuspend")
  suspend fun updateKvSuspend(
    id: String,
    value: String,
    versionCheck: String? = null,
    requestOptions: RequestOptions? = null
  ): ObjectWithoutValue =
    withContext(Dispatchers.IO) {
      updateKv(id, value, versionCheck, requestOptions)
    }

  /**
   * Delete an object
   *
   * Delete an encrypted object.
   *
   * @param id Unique identifier of the object.
   * @param versionCheck Expected current version for optimistic locking.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   */
  @JvmOverloads
  fun deleteKv(
    id: String,
    versionCheck: String? = null,
    requestOptions: RequestOptions? = null
  ) {
    val params = mutableListOf<Pair<String, String>>()
    params.addIfNotNull("version_check", versionCheck)
    val config =
      RequestConfig(
        method = "DELETE",
        path = "/vault/v1/kv/${encodePathSegment(id)}",
        queryParams = params,
        requestOptions = requestOptions
      )
    workos.baseClient.requestVoid(config)
  }

  /**
   * Coroutine-aware variant of [deleteKv]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [deleteKv] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("deleteKvSuspend")
  suspend fun deleteKvSuspend(
    id: String,
    versionCheck: String? = null,
    requestOptions: RequestOptions? = null
  ) = withContext(Dispatchers.IO) {
    deleteKv(id, versionCheck, requestOptions)
  }

  /**
   * Describe an object
   *
   * Fetch metadata for an object without decrypting it.
   *
   * @param id Unique identifier of the object.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the ObjectWithoutValue
   */
  @JvmOverloads
  fun listKvMetadata(
    id: String,
    requestOptions: RequestOptions? = null
  ): ObjectWithoutValue {
    val config =
      RequestConfig(
        method = "GET",
        path = "/vault/v1/kv/${encodePathSegment(id)}/metadata",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, ObjectWithoutValue::class.java)
  }

  /**
   * Coroutine-aware variant of [listKvMetadata]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [listKvMetadata] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("listKvMetadataSuspend")
  suspend fun listKvMetadataSuspend(
    id: String,
    requestOptions: RequestOptions? = null
  ): ObjectWithoutValue =
    withContext(Dispatchers.IO) {
      listKvMetadata(id, requestOptions)
    }

  /**
   * List object versions
   *
   * Retrieve all versions for a specific object.
   *
   * @param id Unique identifier of the object.
   * @param requestOptions per-request overrides (idempotency key, API key, headers, timeout)
   *
   * @return the VersionListResponse
   */
  @JvmOverloads
  fun listKvVersions(
    id: String,
    requestOptions: RequestOptions? = null
  ): VersionListResponse {
    val config =
      RequestConfig(
        method = "GET",
        path = "/vault/v1/kv/${encodePathSegment(id)}/versions",
        requestOptions = requestOptions
      )
    return workos.baseClient.request(config, VersionListResponse::class.java)
  }

  /**
   * Coroutine-aware variant of [listKvVersions]. Use this from
   * a `suspend` function or coroutine scope.
   *
   * Delegates to the blocking [listKvVersions] under
   * `withContext(Dispatchers.IO)`, so this is safe to call from any
   * coroutine dispatcher (including `Dispatchers.Main`).
   */
  @JvmName("listKvVersionsSuspend")
  suspend fun listKvVersionsSuspend(
    id: String,
    requestOptions: RequestOptions? = null
  ): VersionListResponse =
    withContext(Dispatchers.IO) {
      listKvVersions(id, requestOptions)
    }
}
