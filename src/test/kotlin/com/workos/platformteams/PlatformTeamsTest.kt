// This file is auto-generated by oagen. Do not edit.

package com.workos.platformteams

import com.github.tomakehurst.wiremock.client.WireMock.matchingJsonPath
import com.github.tomakehurst.wiremock.client.WireMock.postRequestedFor
import com.github.tomakehurst.wiremock.client.WireMock.urlPathMatching
import com.workos.common.exceptions.GenericServerException
import com.workos.common.exceptions.NotFoundException
import com.workos.common.exceptions.RateLimitException
import com.workos.common.exceptions.UnauthorizedException
import com.workos.test.TestBase
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Assertions.assertNotNull
import org.junit.jupiter.api.Assertions.assertThrows
import org.junit.jupiter.api.Test

class PlatformTeamsTest : TestBase() {
  private fun api() = PlatformTeams(createWorkOSClient())

  @Test
  fun `createTeam returns a typed response`() {
    stubResponse(
      "POST",
      "/platform/teams",
      200,
      "{\"object\": \"team\", \"id\": \"sample\", \"name\": \"sample\", \"production_state\": \"Active\", \"production_enabled_at\": " +
        "null, \"created_at\": \"2024-01-01T00:00:00Z\", \"updated_at\": \"2024-01-01T00:00:00Z\"}"
    )
    val result = api().createTeam("sample-arg", "sample-arg")
    assertNotNull(result)
    assertEquals("team", result.objectType)
    assertEquals("sample", result.id)
    assertEquals("sample", result.name)
    wireMockRule.verify(
      postRequestedFor(urlPathMatching("/platform/teams"))
        .withRequestBody(matchingJsonPath("\$.admin_email"))
        .withRequestBody(matchingJsonPath("\$.name"))
    )
  }

  @Test
  fun `getTeam returns a typed response`() {
    stubResponse(
      "GET",
      "/platform/teams/sample-arg",
      200,
      "{\"object\": \"team\", \"id\": \"sample\", \"name\": \"sample\", \"production_state\": \"Active\", \"production_enabled_at\": " +
        "null, \"created_at\": \"2024-01-01T00:00:00Z\", \"updated_at\": \"2024-01-01T00:00:00Z\"}"
    )
    val result = api().getTeam("sample-arg")
    assertNotNull(result)
    assertEquals("team", result.objectType)
    assertEquals("sample", result.id)
    assertEquals("sample", result.name)
  }

  @Test
  fun `createTeam translates 401 to UnauthorizedException`() {
    stubResponse("POST", "/platform/teams", 401)
    assertThrows(UnauthorizedException::class.java) {
      api().createTeam("sample-arg", "sample-arg")
    }
  }

  @Test
  fun `createTeam translates 404 to NotFoundException`() {
    stubResponse("POST", "/platform/teams", 404)
    assertThrows(NotFoundException::class.java) {
      api().createTeam("sample-arg", "sample-arg")
    }
  }

  @Test
  fun `createTeam translates 429 to RateLimitException`() {
    stubResponse("POST", "/platform/teams", 429)
    assertThrows(RateLimitException::class.java) {
      api().createTeam("sample-arg", "sample-arg")
    }
  }

  @Test
  fun `createTeam translates 500 to GenericServerException`() {
    stubResponse("POST", "/platform/teams", 500)
    assertThrows(GenericServerException::class.java) {
      api().createTeam("sample-arg", "sample-arg")
    }
  }
}
